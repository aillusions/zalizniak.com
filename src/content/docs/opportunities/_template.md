---
title: Opportunity Name
description: One-line statement — the painful workflow + what an AI agent would own (used for SEO + cards).
---

<!--
OPPORTUNITY PAGE — a forward-looking, evidenced thesis on ONE industry/workflow
where an AI agent could own high-value pain nobody has built for yet.

Forward-looking in SHAPE (like the playbook), teardown-grade in EVIDENCE: these
pages make NEW external claims (market size, how the work is done today, who the
incumbents are), so every factual claim carries a tier — VERIFIED / INFERRED /
SPECULATIVE — exactly as in the teardowns. Supporting quotes go in
evidence/opp-<slug>-evidence-map.md (tracked).

Abstract away from any specific product. Write about "an AI agent over documents
and multi-party workflows," never about a named platform.

This file is prefixed "_" so Astro doesn't publish it — copy it to <slug>.md.

Section order, kept consistent so pages compose into a comparative map:
  Overview → The mess → Why now → The money → How it works today →
  Where an agent fits → Whitespace & incumbents → Hard problems → Sources
-->

<!-- OVERVIEW — NO "## Overview" heading. 2–4 sentences directly under the title:
     what the opportunity is + the sharpest hook (the messy workflow an agent
     could own). Then the Vitals strip. Then all heavier market/business context
     folded into a collapsed <details>, never deleted, just one click away. -->

The opportunity: …

**Vitals:** market ~$X · `<volume signal>` · buyer: `<who pays>` · model: `<contingency / subscription / per-transaction>` · whitespace: ★★☆

<details>
<summary>Market context — size, players, money flow</summary>

<!-- TAM/SAM with sources, how money moves through the workflow, who profits
today, regulatory backdrop. Tier-cited. -->

</details>

## The mess

<!-- 2–4 bullets, no intro prose. Each = a concrete broken/manual step in the
workflow today + the specific cost it imposes (money lost, time, error, penalty).
This is the thing an agent would own. Ban promo register. Tier-cited. -->

## Why now

<!-- Tight prose. The tailwind that makes this ripe AND newly solvable: a
demographic/regulatory/market shift + the model-capability inflection. Why it
stayed unsolved, and what changed. 1–2 short paragraphs. -->

## The money

<!-- Table or tight prose. Deal size × volume, the business model, and whether
you can bill from money found/saved (contingency) vs. a budget line. Unit-economics
signal if any. Tier-cited. -->

| Signal | Figure | Basis |
| --- | --- | --- |

## How it works today

<!-- The manual process as it runs now — the multi-party, paper/portal/phone flow.
At least one themed Mermaid diagram of the CURRENT messy workflow
(diagrams/opportunities/<slug>-today.mmd → rendered SVG, source collapsed below).
Render with diagrams/mermaid-theme.json. Short prose carries the reader through. -->

## Where an agent fits

<!-- The agent thesis: which steps an agent owns vs. where a human gates; act-not-
suggest; durable long-running workflow; audit trail. Link the relevant
/ai-playbook/ patterns (assistant-to-actor, durable workflows, integrating systems
without APIs, encoding domain rules) instead of re-explaining them. A second
themed Mermaid diagram of the PROPOSED agent flow. -->

## Whitespace & incumbents

<!-- Who serves this today — legacy software, boutique consultants, manual labor —
and exactly why they're weak/uncrowded. The competitive map. Honest about who's
already circling. Tier-cited. -->

## Hard problems

<!-- Compact table (not prose). 2–4 challenges genuinely hard for an agent in THIS
domain — data access, accuracy bar, regulation, integration with systems-of-record.
Columns: Problem | Why it's hard here | Signal | Likely approach (speculative,
hedged). Tabularizes into a later cross-opportunity map. -->

| Problem | Why it's hard here | Signal | Likely approach (speculative) |
| --- | --- | --- | --- |

## Sources

<!-- Live source links, each backing a claim recorded in the evidence map. One
compact line at the bottom: "Reconstructed from public sources; see confidence
tiers." Pointer to evidence/opp-<slug>-evidence-map.md. -->
